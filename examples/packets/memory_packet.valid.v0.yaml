# This is a non-canonical illustrative example.
# It is provided only for validator first-run usage.
# It is not a packet schema.
# It is not canonical truth.
# promotion_candidate is intentionally quoted to avoid YAML 1.1 boolean coercion.

packet_uid: example-memory-packet-valid-v0-001
packet_type: memory_packet
schema_version: v0
created_at: "2026-05-26T00:00:00Z"
created_by: example_author
owner: example_owner
supervision_mode: human_available_delegate
risk: r0_trivial
status: captured

memory_kind: tooling_note
memory_scope: session_local
summary: Illustrative reusable note captured during validator first-run exercise; not promoted, not canonical.
structured_payload:
  observation: example memory payload captured for first-run validator coverage
  notes:
    - keep this fixture minimal and non-canonical
    - update only when the memory_packet schema changes require it
source_refs:
  - source_type: file
    source_path: examples/packets/task_packet.valid.v0.yaml
    relation: read_from
invalidation_triggers:
  - the cited source file is removed, moved, or renamed
  - the memory_packet schema changes in a way that affects required fields or enums
confidence: 0.5
promotion_candidate: "no"
promotion_notes: This is an illustrative example only and is not a promotion candidate.
