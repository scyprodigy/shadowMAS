# 04_runtime/rejected README | placeholder: packets a human rejected; retained for trace, not for reuse
# related: [SHADOWMAS-RUNTIME-LOADING-MAP, SHADOWMAS-CURRENT-TRUTH, deferred_state_inventory]
# phase: placeholder_deferred

This directory is intentionally empty in v0.

Declared role: packets a human rejected; retained for trace, not for reuse.

The R-layer runtime substrate that would populate it is deferred
(see CURRENT-TRUTH "Still Not Final" and the deferred inventory).
This placeholder exists so the declared directory survives a fresh
clone and is not recreated under a different name. Do not place
artifacts here until the runtime contract that owns this directory
is approved.
